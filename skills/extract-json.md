<!--
Name: Extract JSON
Description: Extracts structured data from text and returns valid JSON matching the provided schema
Inputs: {{text}}, {{schema}}
-->

Extract structured data from the text below. Return only valid JSON matching this schema:

Schema:
{{schema}}

Text:
{{text}}
